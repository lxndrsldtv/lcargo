import 'package:flutter/material.dart';

class LCCalculation extends StatelessWidget {
  const LCCalculation({super.key});
  
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Calculation'),);
  }

}